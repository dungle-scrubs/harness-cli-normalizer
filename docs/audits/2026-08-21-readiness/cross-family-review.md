> Cross-family review by muse-spark-1.2-contributor via hcn run muse. Registry warning at selection: no candidate meets the high-stakes minimums for code-review; treat as below the bar and verify.

| id | verdict | reason |
| --- | --- | --- |
| F-01 | upheld | `src/interpretation/session-id.ts:24` extends `Error` not `ArgvRefusalError`; `src/interpretation/argv.ts:147` and `src/cli/run.ts:355` and `src/execution/stream-turn.ts:188` catch only `ArgvRefusalError` so `SessionIdRefusalError` escapes with no `failure`/`done` and CLI exits 1 with stack trace. |
| F-02 | upheld | `src/knowledge/matchers.ts:23` pattern `429` is bare with no boundary; `src/execution/stream-turn.ts:491` suppresses native classification when `state.limitSeen` is true, so a UUID or byte count containing `429` fabricates `rate-limit` and hides the real error. |
| F-03 | upheld | `src/cli/run.ts:37,92,102,107,134,139,159,178,204,234,247,365` all do `process.stderr.write` + `process.exitCode=2` without consulting `wantJson` at `src/cli/run.ts:111`; under `--json` stdout is 0 bytes with no `failure`/`done`. |
| F-04 | upheld | `src/execution/node-deps.ts:140` resolves `exited` to 127 and puts ENOENT on stderr; `src/execution/stream-turn.ts:486` then builds `failureFromNative` `retryable:false` `cause:crash` instead of `failureFromTransport` `cause:failed`. |
| F-05 | upheld | `src/execution/node-deps.ts:144` drops `signal`; `src/execution/stream-turn.ts:514` ladder maps `exitCode 143` to `cause:crash` `transport`; `src/cli/run.ts:426` `onSig` sets no flag to reach `cause:killed` so deliberate SIGTERM is reported as crash. |
| F-06 | upheld | `src/execution/decode.ts:39` parse-failure branch returns only `{kind:limit}`; `src/execution/stream-turn.ts:362` never pushes that `limit` into `failures`, so `reduceFailures` is empty and `done` is `cause:limit` with `failure` undefined. |
| F-07 | upheld | `src/interpretation/content.ts:68` and `:120` emit only `{kind:error}` for `result.is_error` and `stopReason:error`; no `failure` is pushed, so `src/execution/stream-turn.ts:514` leaves `cause:clean` with no `failure` at exit 0. |
| F-08 | upheld | `src/interpretation/content.ts:63` turns every non-init `system` record into `progress`; a `SessionStart` hook therefore emits 9 to 20 `progress` events before `identity`, breaking `README` line-1 `identity` promise. |
| F-09 | upheld | `src/execution/stream-turn.ts:258` calls `decodeLine` with `opts.model ?? ""`; `src/interpretation/capabilities.ts:30` degrades unknown `""` to `streaming:none` `session:false` `source:unknown` even while `token` events follow. |
| F-10 | upheld | `src/execution/failure.ts:112` `failureFromLimit` never sets `resetsAt`; only `src/execution/decode.ts:88` sets it on `rate_limit_event` with `status != allowed`, so stderr walls for all six `LimitCode` values carry `resetsAt: undefined`. |
| F-11 | upheld | `src/interpretation/tool-selection.ts:118` maps lowercase `read,grep,find,ls` to empty `mapped`, then emits `--allowedTools` with those names and `--disallowedTools` with all 13 builtins; `src/interpretation/argv.ts:126` discards `unmapped` with no warning. |
| F-12 | upheld | `src/interpretation/resolve-options.ts:165` expands `tools: all-known` at `tier:profile` without checking `effectiveArgs.discovery.tools===false`; with `-nt` the tail then appends `--tools read,bash,edit,write,grep,find,ls` and re-enables every builtin. |
| F-13 | upheld | `README.md:189` lists 8 `FailureClass` vs 10 in `src/execution/failure.ts:19`; `README.md:190` lists 6 `ExitCause` vs 7 in `src/execution/events.ts:20`; `retryableOf` at `src/execution/failure.ts:54` is false for five classes, README lists three. |
| F-14 | downgrade to minor | `~/.agents/skills/choose-model/SKILL.md:114` imports package root; `package.json:14` has no `exports`/`main`/`types` so TS2307 is real, but charter puts both Q1 and Q2 on CLI surface only, so library import is outside readiness gate. |
| F-15 | upheld | `src/cli/run.ts:240` gates `resolveEffectiveOptions` on `extra.resume===undefined`; `src/cli/inspect.ts:186` builds resume preview from `fullOpts` so `sandbox:workspace-write` hits `unsupported-on-resume` in preview but not in real `buildResumeArgv`. |
| F-16 | upheld | `src/cli/run.ts:186` calls `resolveSkillNames` and appends `claudeSkillTokens`; `src/cli/inspect.ts:147` calls `buildLaunchArgv` directly with raw `skills` and never resolves names, so preview shows bare names and omits the claude `skillOverrides` tokens. |
| F-17 | upheld | `src/execution/stream-turn.ts:473` and `src/execution/open-session.ts:373` emit `question` then `done awaiting-input` with process exit 0 and no idle timer; no doc states the id stays resumable with no answer budget, so consumer must bound wait itself. |
| F-18 | downgrade to note | `scripts/smoke-claude.ts:124` asserts `done.exitCode===127`; `src/execution/stream-turn.ts:545` nulls `exitCode` for `nativeReduced`, so scenario is red; CI never runs smoke (` .github/workflows/ci.yml`), but failure is dev-script vs exit-code contract, not a Q1/Q2 consumer block. |
| F-19 | downgrade to minor | `src/cli/run.ts:401` arms `turnTimeoutMs` from `--timeout`; `src/execution/stream-turn.ts:330` implements SIGTERM/SIGKILL on it, but `grep -rn turnTimeoutMs test/` is empty, so the path is untested yet functional, a coverage gap not a consumer block. |
| F-20 | downgrade to note | `test/fixtures/` has 40 files, 21 unread, and `src/interpretation/content.ts` and `src/interpretation/identity.ts` have no fixture-backed `identity`/`done` for some harnesses; deterministic suite can stay green on a harness event rename, but a harness still streams correctly. |
| F-21 | downgrade to minor | 34 `limitMatchers`/`authMatchers` entries have no fixture match and only one hand-written string test at `test/interpretation/limits.test.ts:1`; no test asserts `class:rate-limit` etc per harness, so a wording change is missed offline but `detectLimitInLine` still runs. |
| F-22 | downgrade to minor | `scripts/check-versions.ts:56` compares `verifiedAgainst` to npm `dist-tags.latest` for `kind:npm`; `installedVersion` at `check-versions.ts:46` is not recorded for claude/codex/pi, so drift is reported against npm not the local binary, a hygiene gap not a turn block. |
| F-23 | upheld | `src/knowledge/pi.ts:37` and `src/knowledge/muse.ts:38` set `resume.onMissing:create`; no code in `src/execution/stream-turn.ts` or `src/execution/events.ts` reads it, so `hcn run pi --resume <stale>` creates a blank session with normal `identity`/`clean` and lucid silently drops context. |
| F-24 | upheld | `src/execution/open-session.ts:449` builds `done` via `cause: limit/crash/clean` with no `failure` import; no `failureFrom*` call exists, so session mode `done.failure` is always undefined while `streamTurn` provides a full `FailureSummary`. |
| F-25 | upheld | `src/interpretation/argv.ts:178` unconditionally spreads `h.launch.baseFlags`; for pi that is `["-p","--mode","json"]`, so `hcn session pi` spawns `pi -p --mode json --mode rpc` contrary to `src/knowledge/pi.ts:47` intent. |
| F-26 | upheld | `src/knowledge/pi.ts:47` sets `sessionMode.idFlag:null` and reads id only via `get_state`; no test or fixture proves that id resumes via `hcn run pi --resume` (`--session-id` in `mode json`) and the spike at `test/fixtures/pi-rpc-spike/` shows `--session` in RPC mode, a different flag. |

Missing findings:

- `src/execution/stream-turn.ts:258` `freshDecodeState(effective.resume ?? null)` records requested id but `decodeIdentity` rotation at `src/interpretation/identity.ts:65` only emits `error` not `failure`, so rotated resume still yields `cause:clean` with no `failure`.
- `src/cli/run.ts:432` `onSig` arms `setTimeout` for `KILL_GRACE_MS` without `clearTimeout`/`unref`, so node event loop is held 5 s even after child exits, observable in lifecycle.
- `src/knowledge/codex.ts:13` `verifiedAgainst 0.147.0` vs installed 0.146.1 and `src/knowledge/claude-code.ts:15` `2.1.233` vs 2.1.238 are stale on day of audit with no open harness-update issue, making provenance claims stale.

Q1 verdict: not ready. Blockers F-01, F-02, F-03 remain; majors F-04, F-05, F-11, F-12 dominate fallback correctness even after blockers.

Q2 verdict: not ready. Blockers F-01, F-02, F-03 remain; a malformed resume gives no `done` so per-turn reducer hangs, and any refusal gives empty stdout.
