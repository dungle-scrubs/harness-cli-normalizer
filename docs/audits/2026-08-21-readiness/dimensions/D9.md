# D9 - Options, config, provenance, tools, skills

Question served: Q1.

## Evidence consulted

Source read (Read tool):

- `src/interpretation/resolve-options.ts:1-215` - `ProvenanceTier`,
  `FloorExceededError`, `ConfigTiers`, `effectiveConfig`,
  `resolveEffectiveOptions`.
- `src/knowledge/profile.ts:1-54` - `DEFAULT_TURN_PROFILE`.
- `src/cli/config.ts:1-184` - `userConfigPath`, `projectConfigPath`,
  `KNOWN_KEYS`, `parseUserConfig`, `loadUserConfig`, `loadProjectConfig`.
- `src/interpretation/turn-options.ts:1-457` - `renderTurnOptions`.
- `src/interpretation/argv.ts:1-140` - `DiscoveryOptions`, `TurnOptions`,
  `turnTail`.
- `src/knowledge/descriptor.ts:70-169` - `TURN_OPTION_KEYS`,
  `DISCOVERY_FACETS`, `OptionRender`, `resolveRender`.
- `src/interpretation/vocabulary.ts:1-75` - `CLEAN_SELECTOR`, `resolveModel`,
  `validateModel`, `validateEffort`.
- `src/interpretation/tool-selection.ts:1-162` - `renderToolSelection`,
  `resolveNames`.
- `src/interpretation/skills-selection.ts:1-74` - `renderSkillsSelection`,
  `claudeSkillOverridesArg`.
- `src/cli/skills-root.ts:1-48` - `skillsRoot`, `listKnownSkills`,
  `resolveSkillNames`.
- `src/cli/inspect.ts:1-208`.
- `src/cli/run.ts:240-300`, `300-420`.
- `src/knowledge/pi.ts:1-169`, `src/knowledge/overrides.ts:1-45`.

Source read (Bash grep/sed, non-cited-file scanning): `src/knowledge/codex.ts`
`turnOptions`+`vocabulary` block, `src/knowledge/muse.ts` `turnOptions`+`tools`
block, `src/knowledge/claude-code.ts` `turnOptions`+`tools` block, `README.md`
lines 35-50 / 90-145 / 150-175 / 221, `~/.agents/skills/hcn/references/reference.md`
lines 20-50 / 108-131, `~/.agents/skills/delegate/SKILL.md` lines 18-50,
`~/.agents/skills/choose-model/SKILL.md` lines 95-140, `scripts/e2e.ts:262-300`,
test files listed below.

Tests read: `test/interpretation/resolve-options.test.ts` (names + bodies
171-300), `test/cli/config.test.ts` (names), `test/interpretation/tool-selection.test.ts`
(names + bodies 66-105), `test/interpretation/skills-selection.test.ts` (names),
`test/knowledge/overrides.test.ts` (names), `test/knowledge/tool-vocabulary.test.ts`
(names).

choose-model: `npx tsx ~/.agents/skills/choose-model/scripts/choose.ts --tasks`,
then one query per task at `{"task":"<t>","stakes":"normal","privacy":"normal"}`
for all ten tasks, plus `--models`, plus one extra query at
`{"task":"implement","stakes":"normal","privacy":"secret"}` to surface the
`qwen3.6-27b` entry named in H14. Raw JSON at `SCRATCH/audit/d9/choose-*.json`,
`SCRATCH/audit/d9/models.json`; the nine distinct normal-privacy entries at
`SCRATCH/audit/d9/distinct-entries.json`.

CLI runs (no harness spawned by any of them):

- `SCRATCH/audit/d9/inspect-matrix.txt` - 12 `inspect <h> --argv --prompt hi ...`
  runs: the ten choose-model entries plus the two H14 variants.
- `SCRATCH/audit/d9/refusals.txt` - 25 `inspect` refusal probes (unknown model,
  unknown effort, per-model effort ladders, discovery facets, sandbox, provider,
  maxSteps, tools).
- `SCRATCH/audit/d9/skills.txt` - 6 `inspect --skills` probes with
  `HCN_SKILLS_ROOT=$HOME/.agents/skills`.
- `SCRATCH/audit/d9/floor.txt` - project-floor and README-example probes under
  `HCN_CONFIG_DIR=SCRATCH/audit/d9-config/nouser` and scratch git roots at
  `SCRATCH/audit/d9-config/{empty,readme,proj}`.
- `SCRATCH/audit/d9/config-hardfail.txt` - 9 config-parse probes under
  `HCN_CONFIG_DIR=SCRATCH/audit/d9-config/c-*`.
- `SCRATCH/audit/d9/provenance.txt` and `SCRATCH/audit/d9/skills-run.txt` - six
  `hcn run` invocations whose arguments refuse at
  `src/cli/run.ts:343` (`buildLaunchArgv` pre-validation), which is before
  `streamTurn` at `src/cli/run.ts:454`. Provenance printing lives at
  `run.ts:257-280`, ahead of that refusal, so these are the only way to read
  provenance without spawning. Proof no harness ran: every capture exits 2 and
  none carries the `spawn: ` line that `run.ts:377` writes. `inspect` prints no
  provenance, which is finding F-D9-3.

`pi --help` was read for the meaning of `-nt`, `--tools`, and `--provider`.
`~/.pi/models.json` was read for the registered provider names. Neither runs a
model.

No `HCN_CONFIG_DIR` experiment touched `~/.config/hcn` or created `.hcn/` in
either repository; all config fixtures live under
`SCRATCH/audit/d9-config/`.

Graph: `mcp__codebase-memory-mcp__check_index_coverage` on the 17 files below.

## Coverage

`project: Users-kevin-dev-harness-cli-normalizer`, generation
2026-08-21T03:32:49Z, index mode full, `generation_matches: true`. Every file
cited reports `status: no_recorded_issue`, `freshness: metadata_match`:

`src/interpretation/resolve-options.ts`, `src/knowledge/profile.ts`,
`src/cli/config.ts`, `src/knowledge/overrides.ts`,
`src/interpretation/turn-options.ts`, `src/interpretation/argv.ts`,
`src/interpretation/tool-selection.ts`, `src/interpretation/skills-selection.ts`,
`src/cli/skills-root.ts`, `src/knowledge/descriptor.ts`,
`src/interpretation/vocabulary.ts`, `src/cli/run.ts`, `src/cli/inspect.ts`,
`src/knowledge/pi.ts`, `src/knowledge/claude-code.ts`, `src/knowledge/codex.ts`,
`src/knowledge/muse.ts`.

No `partial` result. The signal is best-effort, not proof of completeness.

## Criteria

| # | Criterion | Verdict | Evidence |
| - | --------- | ------- | -------- |
| 1 | Precedence `arg > project-config > user-config > profile > harness` in code and both documents; override file in the chain or documented outside it | pass | `resolve-options.ts:100-104,127,141-207`; `README.md:95-99`; `reference.md:126`; live tiers in `d9/floor.txt` and the first config batch; override caveat in F-D9-8 |
| 2 | Every choose-model `turnOptions` key is a `TurnOptions` key with the same value vocabulary; every `selectedModel` passes `validateModel` on its harness | pass | table below; `d9/inspect-matrix.txt` - all 10 entries exit 0 |
| 3 | `--tools` floor and named toolsets behave per `README.md:116-131` | fail | F-D9-1 (README's own tool names muzzle claude), F-D9-6 (empty floor emits an empty argv token) |
| 4 | `--skills` resolves bare names against `HCN_SKILLS_ROOT`, refuses unknowns listing the registry, refuses on codex/muse with a hint | fail | refusals and hints correct on the `run` path (`d9/skills-run.txt`); `inspect` skips resolution entirely (F-D9-4); the unknown-name refusal names the wrong harness (F-D9-5) |
| 5 | Unknown model or effort refuses; no launch on a default | pass | `d9/refusals.txt` - unknown model exits 2 on claude/codex/muse, unknown effort exits 2 on all four, per-model codex ladders enforced; refusal fires at `run.ts:343`, before `streamTurn` at `run.ts:454` |

### Criterion 2 table - model x harness x verdict

Every key choose-model emits (`effort`, `sandbox`, `provider`, `maxSteps`,
`discovery`) is in `TURN_OPTION_KEYS` (`descriptor.ts:79-93`). Every discovery
facet it emits (`tools`, `instructionFiles`, `extensions`, `skills`) is in
`DISCOVERY_FACETS` (`descriptor.ts:101-106`). No key or facet is outside the
closed vocabularies.

| selectedModel | harness | turnOptions passed as flags | verdict |
| - | - | - | - |
| `claude-fable-5` | claude | `--effort high --no-extensions` | accept |
| `claude-fable-5` | claude | `--effort medium --no-extensions` | accept |
| `claude-opus-5` | claude | `--effort high --no-extensions` | accept |
| `claude-opus-5` | claude | `--effort medium --no-extensions` | accept |
| `claude-sonnet-5` | claude | `--effort medium --no-extensions` | accept |
| `gpt-5.6-sol` | codex | `--effort high --sandbox read-only` | accept |
| `gpt-5.6-sol` | codex | `--effort medium --sandbox read-only` | accept |
| `muse-spark-1.2-contributor` | muse | `--effort medium --max-steps 40` | accept |
| `zai/glm-5.2` | pi | `--effort xhigh --no-tools --no-instruction-files --no-extensions` | accept (but see F-D9-2) |
| `qwen3.6-27b` | pi | `--effort medium --provider lmstudio --no-tools --no-instruction-files --no-extensions --no-skills` | accept (but see F-D9-2) |

No refusal on any of the ten. Criterion 2 passes on the key-and-value question.
What the accepted flags then *render* is F-D9-2.

Two composition notes, neither a criterion-2 failure:

- `discovery` is not a config key (`config.ts:56-76`), so choose-model's
  discovery facets can only cross as CLI flags, never through
  `~/.config/hcn/config.json`. `HCN_CONFIG_DIR=.../c-unknown` with
  `{"version":1,"discovery":{...}}` exits 2 with `unknown config key:
  "discovery"`.
- `~/.agents/skills/delegate/SKILL.md:26-27` maps only
  `--model --effort --provider --sandbox`. It drops `maxSteps` and drops
  `discovery` deliberately. `~/.agents/skills/choose-model/SKILL.md:122-124`
  spreads `...pick.turnOptions` wholesale, so the discovery facets do cross on
  that path.

## Findings

### F-D9-1 `src/interpretation/tool-selection.ts:118-122`

- question: Q1
- severity: major
- coverage: `no_recorded_issue`
- failing scenario: a project config carrying the exact `tools` list from
  `README.md:122` (`["read","grep","find","ls"]`) resolves at project tier and
  reaches `renderToolSelection` for claude. None of the four names is in
  `claudeCode.tools.builtins` (`claude-code.ts:190-204`), whose names are
  `Bash, Edit, Glob, Grep, Read, Write, ...`. `resolveNames` puts all four in
  `unmapped`, so `mapped` is empty and `excluded` at line 119 becomes every
  known claude tool. hcn emits `--allowedTools read,grep,find,ls
  --disallowedTools Bash,Edit,Glob,Grep,Read,Write,WebFetch,WebSearch,Monitor,
  Task,Skill,NotebookEdit,LSP` and exits 0. The delegated claude worker has
  every real tool denied and four grants that name nothing. `renderToolSelection`
  returns `unmapped` for exactly this, and `argv.ts:126-132` discards it - no
  provenance line, no warning, no refusal.
- evidence: `SCRATCH/audit/d9/floor.txt`, sections "README project example on
  claude" and "README toolset 'review' on claude"; also
  `SCRATCH/audit/d9/refusals.txt` "claude --tools read,bash" and
  "claude --tools nosuchtool". Every existing test uses the correct per-harness
  case (`test/interpretation/tool-selection.test.ts:67-73` passes
  `["Read","Bash"]` to claude, `:81-84` passes `["read","bash"]` to pi), so the
  cross-harness case is untested.
- smallest change that flips it: in `renderToolSelection`
  (`tool-selection.ts:108-127`), refuse with `unknown-tool-name` when `mapped`
  is empty and `unmapped` is non-empty on a `includeIsStrictAllowlist: false`
  harness, listing `h.tools.builtins`; or have `src/cli/run.ts` print a
  `provenance: unmapped tools = [...]` line from the discarded
  `RenderedToolSelection.unmapped`.

### F-D9-2 `src/interpretation/resolve-options.ts:165-176`

- question: Q1
- severity: major
- coverage: `no_recorded_issue`
- failing scenario: a caller passes only `--no-tools` on pi. The profile key
  `tools: "all-known"` (`profile.ts:42`) is untouched by args and by config, so
  the `all-known` branch expands it to pi's full built-in list at profile tier.
  `renderTurnOptions` then emits `-nt` for `discovery.tools:false` and
  `turnTail` appends `--tools read,bash,edit,write,grep,find,ls`. pi's own help
  reads `-nt` as "Disable all tools by default (built-in and extension)" and
  `--tools` as an allowlist "to enable", so the profile grant re-enables every
  built-in the caller asked to switch off - including `bash`, `edit`, and
  `write`. The same happens on the privacy route: choose-model's
  `qwen3.6-27b` entry asks for `-nt -nc -ne -ns` and hcn ships
  `-nt -nc -ne -ns --tools read,bash,edit,write,grep,find,ls`. Neither
  `README.md` nor `~/.agents/skills/hcn/references/reference.md:44-48` says the
  profile grant survives `--no-tools`.
- evidence: `SCRATCH/audit/d9/inspect-matrix.txt` entries 9 and 10;
  `SCRATCH/audit/d9/provenance.txt` "pi, unknown effort" shows the profile tier
  supplying the list. `grep -rn "tools: false" test/` returns nothing, so no
  test sets `discovery.tools:false` at all.
- smallest change that flips it: in `resolveEffectiveOptions`
  (`resolve-options.ts:165-176`), skip the `all-known` expansion when
  `effectiveArgs.discovery?.tools === false`, and record the skip as a
  provenance entry.

### F-D9-3 `src/cli/inspect.ts:147-204`

- question: Q1
- severity: minor
- coverage: `no_recorded_issue`
- failing scenario: `inspect` calls `resolveEffectiveOptions` at line 149 and
  destructures only `resolved.options` at line 158. `provenance` and
  `unrenderable` are dropped. `hcn inspect claude --argv --prompt hi --effort
  high` writes one `argv: ` line to stderr and nothing else, so the only
  no-spawn command in the CLI prints no tier for any resolved key and no
  `divergence:` line for `sandbox` on claude, pi, or muse. An agent that wants
  to know which tier set a value must spend a live turn on `hcn run`, which does
  print both (`run.ts:257-280`).
- evidence: `SCRATCH/audit/d9/provenance.txt` - `run` prints seven
  `provenance:` lines plus `divergence: profile "sandbox" not expressible on
  claude` for the same resolution that `inspect` prints nothing for; the
  `inspect` stderr capture in this file's Evidence section shows the single
  `argv:` line.
- smallest change that flips it: in `inspect` (`inspect.ts:158`), write the same
  `provenance:` and `divergence:` lines `run.ts:257-280` writes, or add
  `--provenance` to `INSPECT_HELP` and gate them on it.

### F-D9-4 `src/cli/inspect.ts:113-125`

- question: Q1
- severity: major
- coverage: `no_recorded_issue`
- failing scenario: `run.ts:186-199` resolves `--skills` names to absolute paths
  through `resolveSkillNames` and builds `__claudeSkillTokens` through
  `claudeSkillOverridesArg`. `inspect` does neither. So
  `hcn inspect pi --argv --prompt hi --skills bogus-name` exits 0 and emits
  `-ns --skill bogus-name` - a bare name where the run would emit an absolute
  path, and no refusal where the run refuses. And
  `hcn inspect claude --argv --prompt hi --skills hcn` emits no skill tokens at
  all, because `renderSkillsSelection` returns `[]` for claude
  (`skills-selection.ts:56`) and the `--settings skillOverrides` pair only ever
  gets appended in `run.ts:348-352`. The delegate skill tells the caller to use
  `hcn inspect <harness>` to check flags
  (`~/.agents/skills/delegate/SKILL.md:25-26`); for `--skills` the preview is
  not the argv the run builds.
- evidence: `SCRATCH/audit/d9/skills.txt` (all six inspect probes) against
  `SCRATCH/audit/d9/skills-run.txt` (the same unknown name refuses under `run`).
- smallest change that flips it: call `resolveSkillNames` and
  `claudeSkillOverridesArg` in `inspect` before `buildLaunchArgv`, the same way
  `run.ts:186-199` does, and append the claude tokens to the previewed argv.

### F-D9-5 `src/cli/skills-root.ts:38-44`

- question: Q1
- severity: minor
- coverage: `no_recorded_issue`
- failing scenario: `resolveSkillNames` hardcodes `harness: "claude"` and
  `issue: "unknown-tool-name"` regardless of the harness in play. A caller
  running `hcn run pi --prompt hi --skills bogus-name` reads "claude cannot
  compute a tool complement around an unknown name (unknown skill name(s):
  bogus-name) ... exclude only curated names, or pass the unknown name through
  an include list instead". The harness named is wrong, and the remedy prose is
  the tool-complement prose, not a skills remedy. The registry listing that
  follows is correct and complete, so the refusal is recoverable.
- evidence: `SCRATCH/audit/d9/skills-run.txt`, first section.
- smallest change that flips it: give `resolveSkillNames` the harness name as a
  parameter and add a dedicated `unknown-skill-name` issue to
  `src/interpretation/refusal.ts` with skills-shaped remedy text.

### F-D9-6 `src/interpretation/resolve-options.ts:148-151`

- question: Q1
- severity: minor
- coverage: `no_recorded_issue`
- failing scenario: `README.md:129-131` says "An empty floor refuses every
  grant". An explicit `--tools read` against `{"tools": []}` does refuse
  (exit 2, `FloorExceededError`). With no `--tools` arg the floor check at
  `resolve-options.ts:132` is skipped, and `sourceTier("tools")` returns
  `project-config` for `[]`, so `resolved.tools = []` flows into
  `renderToolSelection` as an empty include list. hcn then emits
  `--allowedTools "" --disallowedTools <all 13>` on claude and `--tools ""` on
  pi - an empty argv token rather than an omitted flag. Exit 0.
- evidence: `SCRATCH/audit/d9/floor.txt` sections "empty floor, no --tools
  (claude)" and "(pi)". `test/interpretation/resolve-options.test.ts:171-187`
  asserts `r.options.tools` equals `[]` and never renders the argv.
- smallest change that flips it: in `renderToolSelection`
  (`tool-selection.ts:108`), treat an empty include list as "deny everything" -
  emit the deny complement without an empty include token on claude, and refuse
  on pi where `--tools ""` has no defined meaning.

### F-D9-7 `README.md:41`

- question: Q1
- severity: minor
- coverage: n/a (documentation line; the code line is `src/knowledge/pi.ts:112`,
  `no_recorded_issue`)
- failing scenario: `README.md:41` shows
  `hcn run pi "name three primes" --provider zai/glm-5.2`. pi's `provider` spec
  is `kind: "selector"` (`pi.ts:112`), validated only against `CLEAN_SELECTOR`
  (`vocabulary.ts:21`), which `zai/glm-5.2` matches. So hcn accepts it and
  passes `--provider zai/glm-5.2` to pi, whose `--provider` takes a provider
  name ("Provider name (default: google)" in `pi --help`; the registered names
  in `~/.pi/models.json` are `lmstudio`, `meta`, `openrouter`, and so on). The
  model id belongs in `--model`, where `pi.ts:76` curates it. Both spellings
  exit 0 in `inspect`, so nothing tells the caller which one is right.
- evidence: `SCRATCH/audit/d9/inspect-matrix.txt` entries "H14a pi glm as
  provider only" and "H14b pi glm both", both exit 0; `pi --help` provider line;
  `~/.pi/models.json` provider keys.
- smallest change that flips it: correct `README.md:41` to
  `--model zai/glm-5.2`. A tighter fix also narrows pi's `provider` spec from
  `selector` to an enum of the descriptor's known provider names, so a
  model-shaped value refuses.

### F-D9-8 `src/knowledge/overrides.ts:229`

- question: Q1
- severity: note
- coverage: `no_recorded_issue`
- failing scenario: `parseOverrides` is exported from `src/knowledge/index.ts:21`
  and called by nothing under `src/cli/` or `src/execution/`. `grep -rl
  "overrides.json" dist/` returns nothing. The path
  `~/.config/harness-cli/overrides.json` appears only as a test constant
  (`test/knowledge/overrides.test.ts:5`) - and it is a different directory from
  the `~/.config/hcn/` the config tiers use. So the override file is not a
  fourth tier, and it is not documented as outside the chain either:
  `README.md:221` describes `parseOverrides` as a library call under the
  concepts section without naming a path or relating it to the precedence chain.
  Criterion 1 passes on precedence; this sub-clause is unsatisfied on the
  documentation half.
- evidence: the greps above; `README.md:221`; no mention of `overrides` in
  `AGENTS.md` or `~/.agents/skills/hcn/SKILL.md`.
- smallest change that flips it: add one line under `README.md`'s "Defaults,
  config, provenance" saying `parseOverrides` is a library-only descriptor
  patch with no file the CLI reads, so the chain is exactly the four tiers
  shown.

### F-D9-9 `src/cli/run.ts:271-279`

- question: Q1
- severity: minor
- coverage: `no_recorded_issue`
- failing scenario: the branch prints "divergence: setting-sources isolation
  also skips hooks, LSP and keychain reads on claude - weighed, not refused"
  when `discovery.instructionFiles === false` on claude. claude's descriptor
  declares no `instructionFiles` facet (only `extensions` and `skills`, at
  `claude-code.ts:160-183`), so `renderTurnOptions` refuses that facet at
  `turn-options.ts:106-116`. The run therefore prints "weighed, not refused" and
  then refuses two lines later with exit 2. The advice line describes a spelling
  the CLI will not build.
- evidence: `SCRATCH/audit/d9/provenance.txt` was captured before this probe;
  the probe output is in this report's Evidence trail:
  `hcn run claude --prompt hi --no-instruction-files` prints the divergence line,
  then `claude cannot express discovery facet "instructionFiles"`, exit 2.
- smallest change that flips it: either add an `instructionFiles` facet to
  `claudeCode.turnOptions.discovery` rendering `--setting-sources project` (it
  is already the `extensions` spelling), or delete the branch at
  `run.ts:271-279` and keep the text in the refusal hint at
  `src/interpretation/hints.ts`, where it already appears.

## Pre-observed

- H14: **confirmed and resolved** (F-D9-7). `zai/glm-5.2` is accepted as
  `--model` on pi and *also* accepted as `--provider` on pi; neither is
  refused. Only `--model` is correct: `piCli.vocabulary.models`
  (`pi.ts:76`) curates `zai/glm-5.2`, and pi's `--model` documents
  `provider/id` form, while pi's `--provider` takes a bare provider name.
  choose-model puts it in `selectedModel`, which the delegate skill maps to
  `--model` - that path is right. `README.md:41` puts it in `--provider` -
  that path is wrong and silently accepted. The glm selection carries no
  `provider` key at all, so no `TurnOptions.provider` value is involved.
  The one entry that does carry `provider` is `qwen3.6-27b` with
  `provider: "lmstudio"`, a real provider name.
- H13: **partly touched, confirmed for `--skills`**. `README.md`'s flag table
  (lines 58-89) has no `--skills` row; `~/.agents/skills/hcn/references/reference.md:42`
  documents the dimension. Full resolution of H13 belongs to D12.

No other hypothesis falls in this section.

## Spend

None. No harness was spawned. Every `hcn run` in this pass refused at
`src/cli/run.ts:343` before `streamTurn` at `src/cli/run.ts:454`; each capture
exits 2 and none contains the `spawn: ` line `run.ts:377` writes. No line was
appended to `SCRATCH/audit/spend.md`.

## Gaps

- The provenance and skills-resolution behaviour of `hcn run` was read from
  refusal-only invocations. A successful launch was never observed, so the
  ordering of `provenance:` lines relative to the `spawn:` line on a run that
  actually spawns is inferred from `run.ts:257-280` and `run.ts:374-387`, not
  observed.
- Criterion 4's sub-question "a skill that exists for one harness only" has no
  subject: `skillsRoot()` (`skills-root.ts:16-17`) is a single flat directory
  shared by pi and claude, and `listKnownSkills` reads no per-harness marker. So
  no skill can exist for one harness only, and the unknown-name path does not
  cover such a case because the case cannot arise. Codex and muse refuse the
  whole dimension before any name is looked up.
- The real-world effect of `-nt` together with `--tools <list>` on pi 0.84.2
  (F-D9-2) is taken from `pi --help` text, not from a live pi turn. The argv hcn
  builds is observed; what pi does with it is not.
- choose-model's own registry facts (that `gpt-5.6-sol` caps at `high` while
  `codex.ts:71` allows `max`; that `qwen3.6-27b` is not a model id registered
  under the `lmstudio` provider in `~/.pi/models.json`) were noticed but are
  registry questions, not normalizer questions, and are out of this section's
  scope.

## Repo state

```
?? 1
```

`git -C /Users/kevin/dev/harness-cli-normalizer status --short` at the end of
this pass shows exactly the one expected untracked line.
