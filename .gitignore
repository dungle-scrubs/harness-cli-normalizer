node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.lefthook-local/
.smoke/

# real-harness smoke fixtures are kept (evidence); .smoke run output is not

# Agent instruction files: tracked shared siblings are NOT ignored; only the
# .local.md siblings are local-only and must stay untracked at every scope.
AGENTS.local.md
CLAUDE.local.md

# lucid review records: artifacts are committed deliberately; run/ is machine-local
.lucid/**/run/
# e2e run output (evidence rerun, not kept)
.e2e/
